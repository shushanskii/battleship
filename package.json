{
  "name": "battleship-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "agent": "npm start --workspace=packages/agent",
    "frontend": "npm run dev --workspace=packages/frontend"
  }
}
